--country with the highest consumptionof beer--
SELECT t1.countries, SUM(t1.quantity_sum) AS beer_consumption
FROM 
	(SELECT brands, countries, SUM(quantity)  AS quantity_sum
	 FROM int_brew 
	 GROUP BY 1,2
	 HAVING brands != 'beta malt' AND brands != 'malt') t1
	GROUP BY 1
ORDER BY 2 DESC;